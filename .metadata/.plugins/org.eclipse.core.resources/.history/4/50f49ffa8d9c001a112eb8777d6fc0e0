import java.util.ArrayList;

public class maze {
	int rows;
	int cols;
	ArrayList<ArrayList<mazeNode>> graph;
	
	public maze (int _rows, int _cols, ArrayList<ArrayList<mazeNode>> _graph) {
		rows = _rows;
		cols = _cols;
		graph = _graph;
	}
}
